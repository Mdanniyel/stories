---
// src/layouts/Layout.astro
import '../styles/global.css'; // כאן אנחנו טוענים את Tailwind לכל האתר!

interface Props {
	title: string;
}

const { title } = Astro.props;
---

<!doctype html>
<html lang="he" dir="rtl">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/svg+xml" href=`${import.meta.env.BASE_URL}favicon.png` />
		<meta name="generator" content={Astro.generator} />
		<title>{title}</title>
	</head>
	<body class="bg-gray-50 text-gray-900 font-sans min-h-screen flex flex-col">
		
        <header class="bg-indigo-600 text-white shadow-md p-4 sticky top-0 z-50">
            <div class="container mx-auto flex justify-between items-center max-w-5xl">
                <a href=`${import.meta.env.BASE_URL}` class="text-xl font-bold hover:text-indigo-100 no-underline text-white">
                    📚 ספריית הסיפורים
                </a>
                </div>
        </header>

        <main class="container mx-auto max-w-5xl p-4 flex-grow">
			<slot />
		</main>

        <footer class="bg-gray-200 text-center p-4 mt-8 text-sm text-gray-600">
            נבנה באמצעות Astro & Svelte
        </footer>

	</body>
</html>
